# 笔记

* 一个子进程结束后，内核立即回收一部分资源，并将子进程标记为僵尸进程，随后再继续回收剩余资源。

* waitpid是由父进程调用，等待子进程结束后资源完成回收。

* 前面实验，内核栈空间在os启动之前就分配完毕，每个应用的内核栈空间之间使用guard page隔离，本章中开始需要通过结构维护栈空间，保存对应进程id。
但是并没有动态分配内核栈空间，还是根据id计算偏移。(虚拟地址下)
使用insert_framed_area向KERNEL_SPACE插入MapArea。
